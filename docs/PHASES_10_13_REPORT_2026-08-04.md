# Phases 10-13 report - 2026-08-04

## Executive status

| Phase | Status | Evidence |
|---|---|---|
| 10 cross-provider calibration | Partially complete | Anthropic gate passed; OpenAI live leg blocked by missing credential |
| 11 approval-safe local MCP | Complete locally | official MCP 2.0 server mounts 9 tools; adversarial tests pass |
| 12 two execution proofs | Partially complete | shared OpenAI/Anthropic contract tests and live Anthropic proof pass; live OpenAI and Managed Agents unavailable |
| 13 stability and release | Complete locally | CI definitions, recovery tests, schema policy and clean RC install pass |

No phase added a lifecycle state, agent runtime, network authentication system
or provider field to `HypothesisRecord`.

## Phase 10

The first three Anthropic runs exposed one empty `counterexample`: 23/24
first-pass contracts. Praxis failed closed. The review contract was then
changed to require non-empty text and to tell a reviewer how to construct a
counterexample for a self-sealing claim.

Repeated result after that change:

- provider/model: Anthropic `claude-sonnet-5`, effort `low`
- prompt SHA-256: `a4ddb136630f3e881836afe0b83b849c0bcd58310e20f2668f7843471a12b374`
- first-pass contract validity: 24/24 (100%)
- acceptable recommendations: 24/24 (100%)
- objection-concept recall: 88.24%
- repairable cases classified `REJECT`: 0
- mean request latency: 7.95 s
- estimated three-run cost at supplied 2/10 USD per MTok: 0.173028 USD

Raw responses remain under ignored `.praxis/evaluations/`. The two-provider
exit gate is not passed because `OPENAI_API_KEY` was absent. This is an
environment prerequisite, not evidence that the OpenAI adapter passes.

## Phase 11

`PraxisMCPTools` defines one in-process contract and an optional stdio mount on
the official MCP SDK. Tools are classified as read-only, reversible mutation
or consequential proposal. The catalog contains no `approve` or `decide`.

The host-only `ApprovalBoundary` rejects model-supplied approval evidence,
expires proposals, checks optimistic revision, consumes each proposal once and
leaves no partial decision after denial or stale approval. MCP 2.0 server
construction was verified from a clean optional installation. Remote MCP and
authentication remain deferred.

## Phase 12

One proof harness runs identical bounded `TEST`, repairable `REVISE` and
terminal `REJECT` scenarios against any hostile-review port. Deterministic
tests exercise both provider host shapes and produce equivalent histories.

A live Claude Sonnet 5 execution produced `TEST`, `REVISE -> deflate -> TEST`
and `REJECT/FALSIFIED`, each with explicit host approval and complete evidence
lineage. Provider metadata stayed in the ignored run report. Deleting an
adapter-side `ExecutionReceipt` does not affect record deserialization.

The live OpenAI proof was not run without a credential. The installed
Anthropic SDK/account did not expose Managed Agents, so the successful live
proof uses the stable Messages adapter and must not be described as a Managed
Agents proof.

## Phase 13

- deterministic CI matrix: Python 3.11, 3.12 and 3.13
- paid evaluation: manual workflow with selected secret and hard cost ceiling
- SQLite: schema version 1, WAL, `synchronous=FULL`, integrity check and passive checkpoint
- recovery: committed abrupt-exit data survives; uncommitted transaction rolls back
- pre-release: `matylda-praxis 0.2.0rc1`
- clean wheel SHA-256: `27b6f8cdf3e199d2b417d44018d57ddcceb38a862e8b863b5f5743604be228d3`
- clean environment import and CLI help: passed
- optional MCP install and MCP 2.0 server construction: passed

The base package still has zero runtime dependencies. OpenAI, Anthropic and MCP
remain optional extras.

## Next gate

Supply an OpenAI API credential, query the account-visible models, and run the
same repeated calibration plus three-scenario execution proof. Do not begin
Phase 14 until both that comparison and the required real Praxis runs exist.
